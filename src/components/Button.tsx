// import { motion } from 'framer-motion';

const Button = () => {
    return (
        <button
            className="rounded bg-pink-700 py-4 px-10 font-serif font-medium tracking-wider text-white 
			shadow-[0_0_12px_rgba(255,47,47,0.5)] transition-all duration-500 hover:bg-pink-800 hover:shadow-pink-700">
            Get in touch
        </button>
    );
};

export default Button;
